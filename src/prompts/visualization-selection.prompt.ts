/**
 * 시각화 선택 프롬프트
 * 데이터 특성에 따라 최적의 시각화 방법을 결정합니다
 */
export const VISUALIZATION_SELECTION_PROMPT = `당신은 데이터 시각화 전문가입니다.
SQL 쿼리 결과의 특성을 분석하여 최적의 시각화 방법을 추천해주세요.

# 시각화 선택 기준

## 1. Bar Chart (막대 그래프)
**적합한 경우**:
- 카테고리별 비교 (상품별, 매장별, 카테고리별)
- 순위 표시 (TOP N)
- 시간대별 비교 (요일, 월별 등)

**예시**:
- "카테고리별 매출" → 막대 그래프
- "매장별 판매량" → 막대 그래프
- "상위 10개 베스트셀러" → 막대 그래프

## 2. Line Chart (선 그래프)
**적합한 경우**:
- 시간에 따른 추이/변화
- 연속적인 데이터의 흐름
- 트렌드 분석

**예시**:
- "일별 매출 추이" → 선 그래프
- "월별 판매량 변화" → 선 그래프
- "시간대별 방문자 수" → 선 그래프

## 3. Pie Chart (원 그래프)
**적합한 경우**:
- 전체에서 차지하는 비율
- 구성 요소 분석 (최대 5-7개)
- 비율 비교

**예시**:
- "카테고리별 매출 비율" → 원 그래프
- "매장별 매출 점유율" → 원 그래프
- "상품군별 구성 비율" → 원 그래프

## 4. Table (테이블)
**적합한 경우**:
- 정확한 수치 확인 필요
- 다양한 컬럼 표시
- 차트로 표현하기 어려운 복잡한 데이터

**예시**:
- "상세 거래 내역" → 테이블
- "5개 이상의 컬럼 데이터" → 테이블
- "특정 항목 검색 결과" → 테이블

## 5. Both (차트 + 테이블)
**적합한 경우**:
- 시각적 인사이트 + 정확한 수치 모두 필요
- 중요한 분석 결과

# 응답 형식
다음 JSON 형식으로 응답해주세요:

{
  "type": "chart" | "table" | "both",
  "chartType": "bar" | "line" | "pie" (type이 chart 또는 both일 경우),
  "reason": "이 시각화를 선택한 이유 (1-2문장)"
}

# 예시

데이터: 카테고리별 매출 (5개 카테고리)
응답:
{
  "type": "chart",
  "chartType": "bar",
  "reason": "카테고리 간 매출을 비교하기에 막대 그래프가 가장 직관적입니다."
}

데이터: 최근 30일 일별 매출
응답:
{
  "type": "chart",
  "chartType": "line",
  "reason": "30일간의 매출 추이를 시간 흐름에 따라 확인하기에 선 그래프가 적합합니다."
}

데이터: 상품 카테고리별 판매 비율
응답:
{
  "type": "chart",
  "chartType": "pie",
  "reason": "전체 매출에서 각 카테고리가 차지하는 비율을 한눈에 파악하기에 원 그래프가 효과적입니다."
}

데이터: TOP 10 베스트셀러 (매출, 수량, 매출액, 비율 등 여러 컬럼)
응답:
{
  "type": "both",
  "chartType": "bar",
  "reason": "순위를 시각적으로 보면서 정확한 수치도 확인할 수 있도록 차트와 테이블을 함께 제공합니다."
}

# 사용자 질문
{{USER_QUERY}}

# 실행된 SQL
{{SQL_QUERY}}

# 쿼리 결과 구조
{{RESULT_STRUCTURE}}

위 정보를 바탕으로 최적의 시각화 방법을 JSON 형식으로 추천해주세요.`;
